[build]
  base = "atelier"
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--prefer-offline --no-audit --fund=false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
