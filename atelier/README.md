# atelier — peek.gift vNext

Fresh Next.js 15 + TS strict + Tailwind v3 foundation. The existing app at the repo root is unrelated scrap.

Run: `cp .env.example .env.local`, `npm install`, `npm run dev`.

Build status + packet graph: see `../_packets/STATE.md`.
