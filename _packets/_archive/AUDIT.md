# AUDIT — atelier/

_Generated by packet 29 on 2026-05-26 against `atelier-integration` @ `5376e52`._

Out of scope per orchestrator notes (already fixed on trunk): RLS default-deny (`0005`), Drizzle-as-canonical-types pivot for packet 30, devDeps for vitest/eslint/node-vibrant. Not re-flagged.

## Summary

- 96 total findings
- 13 critical (security / data loss / shipping bugs)
- 41 major (reliability / correctness / scope holes)
- 42 minor (quality / polish)

## Findings (grouped by target packet)

### → Packet 30 (Type safety)

- `atelier/lib/supabase/types.ts:1-12` — `Database = { peek_v2: { Tables: { [tableName: string]: { Row: Record<string, unknown>, Insert: Record<string, unknown>, ... } } } }`. Every `.from('peeks').select(...)` returns `unknown`-shaped rows. Right: derived from Drizzle `$inferSelect`/`$inferInsert` per packet 30 plan. **Cost: every Supabase call needs a follow-up `as RowType`, which is why 22+ ad-hoc `as PeekRow|CardRow|GroupRow|RawPeekRow|etc.` casts exist downstream.**
- `atelier/app/api/chat/schema.ts:6-7` — `history: z.array(z.unknown())` + `userMessage: z.union([z.string(), z.array(z.unknown())])`. Right: `z.array(MessageParamSchema)` + a strict `ContentBlockParamSchema` union. Today the API trusts whatever the browser sends and feeds it raw to Anthropic.
- `atelier/lib/anthropic/tools/index.ts:42` — `TOOL_REGISTRY.set(tool.name, tool as unknown as ToolDefinition)`. Double-cast that widens `Input/Output` to `unknown`. Right: generic-preserving Map via `Map<string, ToolDefinition<unknown, unknown>>` with phantom typing; or store the typed tool and type-narrow at dispatch.
- `atelier/components/recipient/realtime.ts:78,99` + `atelier/lib/peek/realtime.ts:87,104,148` + `atelier/app/build/[peekId]/publish/publish-status.tsx:52` — `'postgres_changes' as never`. Workaround for Supabase realtime channel typing gap. Right: extend `@supabase/supabase-js` declarations or a thin wrapper that types the event name. Five sites.
- `atelier/app/g/[slug]/page.tsx:88,114,169,217,243,245,254` + `atelier/app/build/[peekId]/page.tsx:96,137,159,160` + `atelier/app/build/[peekId]/publish/page.tsx:47` + `atelier/app/build/[peekId]/publish/share/page.tsx:92` + `atelier/app/build/[peekId]/publish/publish-status.tsx:79` + `atelier/app/api/checkout/route.ts:62` + `atelier/app/api/stripe/webhook/route.ts:81` + `atelier/app/api/pick/route.ts:87,102,117` + `atelier/app/api/share/send/route.tsx:127` + `atelier/app/g/[slug]/opengraph-image.tsx:93` + `atelier/lib/chat/session.ts:78` — every Supabase result cast through a hand-rolled `as XxxRow` shape. ~22 sites. Drizzle-typed `Database` (packet 30) auto-deletes all of them.
- `atelier/components/recipient/realtime.ts:78` — `data as unknown as FetchPickRow[]`. Same as above plus an `unknown` waypoint to defeat overload selection.
- `atelier/lib/jobs/nudge-relationships.ts:62` — `rows as unknown as { rows?: NudgeRow[] } | NudgeRow[]`. Drizzle `db.execute<T>(sql\`...\`)` return shape is library-defined; the cast hides whether postgres-js gives `T[]` or `{ rows: T[] }`. Right: use Drizzle's typed builder (`db.select().from(...).where(...)`) instead of raw SQL.
- `atelier/lib/anthropic/tools/set_vibe.ts:206` + `atelier/lib/anthropic/tools/update_vibe.ts:135` — `payload: { source: 'set_vibe', patch: parsed as Record<string, unknown> }`. `parsed` is already a Zod-inferred object; cast is to satisfy the analytics facade's loose `payload` type. Right: tighten `track()` event payloads to the actual zod-derived shape.
- `atelier/lib/anthropic/tools/add_card.ts:111` — `parsed.unlock_rule as UnlockRule`. Already validated by Zod; cast widens vs `UnlockRule | Record<string, never>`. Right: a Zod schema that's a discriminated union of the two states.
- `atelier/app/api/webhooks/skimlinks/route.ts:88` — `evt as unknown as Record<string, unknown>`. Right: drop the cast; `SkimlinksEvent` is already a structural superset that's compatible with `Record<string, unknown>` if the interface is widened with `[k: string]: unknown` or `JSONObject`.
- `atelier/app/g/[slug]/page.tsx:35` + `atelier/app/build/[peekId]/page.tsx:30` + `atelier/lib/chat/session.ts:53` + `atelier/lib/peek/types.ts:39` + `atelier/lib/peek/realtime.ts:29` — `metadata: Record<string, unknown>` as the persistent shape. There IS a concrete metadata sub-key (`anonymous_session_id`), but no `PeekMetadata` interface. Right: `type PeekMetadata = { anonymous_session_id?: string; [key: string]: unknown }` lifted to a single source.
- `atelier/db/schema/peeks.ts:74` + `atelier/db/schema/cards.ts:87` + `atelier/db/schema/events.ts:23` + `atelier/db/schema/affiliate_revenue.ts:22` — `.jsonb('metadata').$type<Record<string, unknown>>()` on persisted columns where the shape is partially known. Right: discriminated `$type<>` per column (per-table metadata shape).
- `atelier/lib/anthropic/tools/scrape_url.ts:14-16` — `Output` union has `affiliate_network?: 'skimlinks' | 'sovrn' | 'direct'` but `wrapAffiliateLink()` returns the same enum on `WrappedLink.network`. Right: import `AffiliateNetwork` from `@/lib/affiliate/wrap`.
- `atelier/lib/peek/types.ts` Vibe vs `atelier/db/schema/peeks.ts` Vibe — STILL divergent (STATE.md flagged it post-packet-21 and it never got followed up). `db/schema/peeks.ts` Vibe has `preset`, `signal_source_history`. `lib/peek/types.ts` Vibe has neither. UI code that consumes the db shape via `Peek['vibe']` works by accident because both shapes have the common fields. Right: re-export Vibe from db schema; delete the UI copy.
- `atelier/lib/peek/types.ts:1-86` whole module duplicates the row-mapped UI shape of every Drizzle table (Peek, Card, VariantGroup, Vibe, etc.). Manual sync drift trap. Right: derive UI types from `typeof peeks.$inferSelect` with snake_case → camelCase mapping helper.
- `atelier/lib/jobs/webhook-logger.ts:13-17` — `webhook_log.payload` is `jsonb` typed as `unknown`, but routes that publish `peek/webhook.received` pass `{ source, payload, success }` where `payload` is a hand-built `Record<string, unknown>`. Drift waiting to happen between event schema and row schema.
- `atelier/db/schema/chat_history.ts:14` — `content: jsonb('content').$type<unknown>()`. `unknown` is the worst case; it should be the Anthropic `MessageParam.content` shape (`string | ContentBlockParam[]`). Right: import from `@anthropic-ai/sdk` and stamp the column.

### → Packet 31 (Security)

- `atelier/app/g/[slug]/page.tsx:131-137` + `atelier/app/api/pick/route.ts:43-49` — **`signRecipient()` fails OPEN**: when `GUEST_CLAIM_TOKEN_SECRET` is missing, returns `unsigned:<sessionId>` and lets the request through. Same plaintext signature is then stored on the `picks.recipient_signature` column, so picks from any session map to a deterministic value. Right: throw or 503 when secret missing. Force env check in `lib/env.ts`. CRITICAL.
- `atelier/lib/env.ts:58` — `GUEST_CLAIM_TOKEN_SECRET: z.string().optional()`. Should be required, with length floor (≥ 32 chars). Same goes for STRIPE_WEBHOOK_SECRET / CLERK_WEBHOOK_SIGNING_SECRET when their respective features are toggled on. **All secrets are `.optional()`**, so production-misconfig errors degrade to "feature off" instead of crashing. CRITICAL.
- `atelier/lib/chat/session.ts:103-119` + `atelier/app/api/chat/route.ts:70-78` — anon turn cap (5) is enforced by `count(*) from events where session_id = X and kind = 'chat_turn' and user_id is null`. Client controls `sessionId` (set in localStorage by `chat-pane.tsx:55-62`). Trivially bypassable: clear localStorage → new `crypto.randomUUID()` → fresh cap. Right: IP-keyed bucket via Upstash + per-fingerprint (UA + IP) and per-anon-peek-id (verifiable via the peek's metadata.anonymous_session_id seed if/when that flow exists). CRITICAL.
- `atelier/app/api/chat/route.ts` + `atelier/app/api/checkout/route.ts` + `atelier/app/api/pick/route.ts` + `atelier/app/api/share/send/route.tsx` + `atelier/app/api/scrape/route.ts` + `atelier/app/api/upload/route.ts` — **zero rate limiting**. Upstash deps installed in `package.json` (`@upstash/ratelimit`, `@upstash/redis`), env vars declared, nothing wired. `lib/rate-limit/redis.ts` doesn't exist on trunk. Right: `chat` (LLM cost), `checkout` (Stripe create), `pick` (anon DB writes), `share/send` (Twilio/Resend cost), `scrape` (Anthropic + Browserbase cost), `upload` (Supabase storage). Per-IP fail-closed when Redis is reachable; fail-open with `console.warn` when not. CRITICAL.
- `atelier/proxy.ts` + `atelier/next.config.mjs` — **no CSP, no security headers**. No `Content-Security-Policy`, `Strict-Transport-Security`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `X-Content-Type-Options`. Inline `<script>` in `next/og` ImageResponse and PostHog autocapture both run unrestricted. Right: `next.config.mjs` `async headers()` block or `proxy.ts` mutating response headers. CRITICAL.
- `atelier/app/api/stripe/webhook/route.ts` + `atelier/app/api/webhooks/clerk/route.ts` + `atelier/app/api/webhooks/skimlinks/route.ts` — **no idempotency keys**. Stripe signed events can be replayed within the 5-min Stripe-signature TTL; Svix sigs likewise. Stripe webhook is conceptually idempotent because of the `peek.status === 'published'` short-circuit on `checkout.session.completed`, but other event types (added later) won't have that. Skimlinks upserts by `external_txn_id` (good), but Clerk delete/update is not idempotent. Right: `idemp:<source>:<event.id>` set-NX-ex in Redis. CRITICAL.
- `atelier/app/api/webhooks/clerk/route.ts:14` + `atelier/app/api/stripe/webhook/route.ts:24` — `} catch {}` silently swallows Inngest send errors. They're not security per se but the comment policy violation (no WHY) AND they hide signal that webhooks are not being logged. Already in `_packets/COMMENTS.md` for `generate_hero_image` but THESE TWO aren't documented. (Major, but tagged here because they're in webhook handlers.)
- `atelier/lib/supabase/service.ts:11-12` + `atelier/lib/supabase/browser.ts:10-11` + `atelier/lib/supabase/server.ts:14-15` — `env.NEXT_PUBLIC_SUPABASE_URL!` and `env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!` and `env.SUPABASE_SERVICE_ROLE_KEY!` — bang-asserted reads of `.optional()` env vars. If unset, runtime throws an opaque "Cannot read properties of undefined" instead of a clean env validation error at boot. CRITICAL boot-config bug. Right: tighten env to require these.
- `atelier/db/migrations/0002_schema_exposure.sql:5,9` — `GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA "peek_v2" TO anon, authenticated`. With RLS now ON (0005), this is the ceiling, not the floor — but anon should never have INSERT/UPDATE/DELETE at all. Right: revoke writes from anon. Restrict to SELECT only on the read-needed tables (peeks for recipient view, cards, variant_groups, picks for the recipient's own row). Service-role does all the writes via server routes.
- `atelier/app/api/webhooks/clerk/route.ts:71-79` — `if (evt.data.id) { db.from('users').delete().eq('clerk_user_id', evt.data.id) }`. Svix already verified signature, so trust is correct, BUT no audit log row + no soft-delete + cascades will nuke peeks/cards/picks/events via FK. Right: soft-delete column or move to "deactivated" flag.
- `atelier/app/build/page.tsx:6-9` — `function makeSlug() { return crypto.randomUUID().replace(/-/g, '').slice(0, 12); }`. 12 hex chars = 48 bits of entropy. With ~100M Peeks the birthday paradox makes collision likely (~10⁻⁴). Insert call doesn't handle the resulting unique-constraint failure. Right: longer slug (16+ chars) AND retry-on-conflict.
- `atelier/app/api/scrape/route.ts:92-95` — scrape requires auth (`getUserId()`), but `peekId` is optional, so an authed user can scrape any URL targeting any peek (no ownership check on `peekId`). Should `assertPeekAccess` when `peekId` is provided. Right: parity with `/api/chat`.
- `atelier/app/api/upload/route.ts:36-37` — anon upload path: `getUserId()` may be null AND `sessionId` taken from `req.cookies.get('peek-anon-session')`. But `chat-pane.tsx:55-62` puts the anon session in **localStorage**, not cookies. So an anon curator's `sessionId` is always `''` here, never matches metadata, returns 403/404. Net: anon upload is broken. Right: pick one (cookie vs localStorage) and use it consistently.
- `atelier/app/api/upload/route.ts:10-19` — `MAX_SIZE = 8 * 1024 * 1024` enforced AFTER reading the full FormData into memory. Right: read `req.headers.get('content-length')` first, reject early. Bigger picture: no storage-bucket-level size limit RLS rule, no per-peek upload count rate limit, so a single curator can fill the bucket.
- `atelier/app/api/share/send/route.tsx:14-20` — `BodySchema.channel` is enum-validated but `destination` is `z.string().min(3)` for both SMS and email. No phone-format check for SMS, no email-format check for email. Right: branch the schema.
- `atelier/lib/security/recipient.ts` exists in the codebase (sibling-agent leakage into parent worktree) BUT IS NOT USED. Real code at `g/[slug]/page.tsx` and `api/pick/route.ts` still has the inline `unsigned:` fallback. Confirm `lib/security/*` isn't on `atelier-integration` HEAD before packet 31 dispatches — it isn't (`git ls-files` confirms). NOTE for orchestrator.

### → Packet 32 (Tests + CI)

- **Zero unit tests.** `find atelier -name '*.test.ts' -o -name '*.spec.ts'` → 0 results. Vitest + coverage-v8 in devDeps (added in batch 4 prep).
- **Zero E2E tests.** `@playwright/test` is in devDeps (added long ago, never used). No `playwright.config.ts`.
- **No GitHub Actions for typecheck/lint/test.** Only `.github/workflows/netlify-deploy.yml` which is a curl to a build hook. Right: PR-gated `npm run typecheck && npm run lint && npm test` on every push.
- **No ESLint config.** `eslint`, `@next/eslint-plugin-next`, `typescript-eslint` all in devDeps but no `.eslintrc.*` or `eslint.config.mjs`. `npm run lint` = `next lint` will use Next's defaults but won't pick up the typescript-eslint plugin chains.
- **No `vitest.config.ts`** either. Worker for packet 32 needs to land both configs from scratch.
- **No preview-deploy gating.** Netlify deploy hook fires on push to `atelier-integration` regardless of test status. Right: GHA job that runs typecheck+test, then dispatches the build hook only on success.

### → Packet 33 (Reliability)

- `atelier/lib/image-gen/fal.ts:107-183` — fal.ai pipeline does THREE fetches (submit, status poll, response). NONE have `AbortController` / timeout. A hung submit blocks the whole tool call. Right: per-call timeout, plus retry-with-jitter on 5xx.
- `atelier/lib/image-gen/rehost.ts:35` — `await fetch(opts.sourceUrl)` no timeout, no retry. fal.ai CDN flap → tool fails permanently. Right: retry 3x with exponential backoff + 10s per-attempt timeout.
- `atelier/lib/scrape/extract.ts:110-120` — Anthropic call from extractProduct has no timeout, no retry. Same pattern through the codebase: every `anthropic.messages.create()` is naked.
- `atelier/lib/anthropic/observability.ts:132` + `atelier/lib/vibe/classify-tone.ts:28` + `atelier/lib/scrape/extract.ts:110` — three more naked `anthropic.messages.create` calls.
- `atelier/app/api/share/send/route.tsx:80-92` — Twilio + Resend wrapped only in try/catch (returns `{ ok:false, error }`). No retry on transient 5xx. Right: retry on idempotent send failure with attempt-id dedup.
- `atelier/app/api/stripe/webhook/route.ts:67-109` — Supabase update is best-effort. If `from('peeks').update(...)` returns an error after Stripe paid, we return 500 → Stripe retries 3× over 3 days → first retry hits the early-out (`already_published`) only if first update DID succeed. If the update actually failed (network), Stripe retries but webhook always returns 500. Right: write to an outbox + reconcile.
- `atelier/lib/anthropic/chat.ts:111` — `MAX_TOOL_ITERATIONS = 10` cap is silent. When hit, route ends without telling user/model the loop hit the wall. The trailing comment ("synthetic note so the model has a chance to wrap up") doesn't actually append a note. Right: append a synthetic `tool_result` with `is_error: true` and a "you've hit the tool budget" message before returning.
- `atelier/lib/anthropic/chat.ts:140` — `if (event.type === 'error')` returns immediately after observability capture. The capture is on the success-path side but the comment says "caller already saw the `error` event" — true for the SSE consumer, but the chat-route's user-message persist already happened, and the assistant-turn persist now never happens, leaving DB inconsistent.
- `atelier/app/api/chat/route.ts:90-93,137-143,160-167,184-192` — multiple `try { ... } catch (err) { console.error(...) }` swallowing patterns. `loadChatHistory` failure shouldn't reset history (it does); `appendChatMessage` failures aren't surfaced to the client. Right: structured logger + at least 1 retry on DB ops + propagate persist-failures as `error` SSE events.
- `atelier/app/error.tsx`, `atelier/app/global-error.tsx`, `atelier/components/error-boundary.tsx` — **these files exist on disk (parent-worktree leakage) but are NOT on the branch HEAD.** Real HEAD has only `global-error.tsx`. There is **no `app/error.tsx`** so any segment crash (e.g. recipient view 500 from `peekRes.error`) bubbles up to `global-error.tsx` which means full-doc reload. Right: per-segment `error.tsx` for `/build`, `/g/[slug]`, `/api/*` (where applicable). All commits to `_packets/COMMENTS.md` for the few comments that land.
- `atelier/lib/anthropic/client.ts:18-21` — boot-time `console.warn` for missing ANTHROPIC_API_KEY. Right: throw at first use; warn is too quiet.
- `atelier/lib/email/send.ts:36-54` — try/catch returns `{ ok:false, error: message }` for everything including auth failures. Caller has no way to distinguish "wrong API key" from "rate limited" without parsing strings. Right: typed error variants.
- `atelier/lib/scrape/pipeline.ts:33-41` — top-level try/catch returns generic `scrape_failed` for everything except an explicit `scrape_timeout` string match. Loses provider/cause. Right: pass through the inner Error's structured cause.
- `atelier/lib/jobs/scrape-worker.ts:27` — Inngest function fetches `${env.APP_URL}/api/scrape` over the public network with no auth header but `/api/scrape` requires authed user (`getUserId()` returning a clerk user). The worker will get 401 every time it runs. Right: call the pipeline directly via `import { scrapePipeline } from '@/lib/scrape/pipeline'`. STATE.md notes this; orchestrator hasn't fixed it. CRITICAL functional bug.
- `atelier/lib/inngest/client.ts:25-29` — Inngest client constructed at import time; no defensive handling for missing event-key. `await inngest.send(...)` will throw on every webhook if Inngest is not configured. The `try {} catch {}` in clerk/stripe webhooks swallows it (good for resilience, bad for visibility).
- `atelier/components/ui/use-toast.ts:9` — `TOAST_REMOVE_DELAY = 1000000` ms (≈ 16 minutes). Stock shadcn copy-paste bug; toasts effectively never auto-remove. Combined with `TOAST_LIMIT = 1`, only one toast ever shows. Right: 4-5 seconds.
- `atelier/components/recipient/realtime.ts:94-140` + `atelier/lib/peek/realtime.ts:78-196` + `atelier/app/build/[peekId]/publish/publish-status.tsx:47-67` — Supabase realtime channels: no reconnection logic, no exponential backoff, no presence to detect disconnect, no UI signal. A Netlify edge restart drops the channel silently.
- `atelier/components/build/chat-pane.tsx:286-336` — SSE reader has no timeout. If the server hangs mid-stream, the user sees "Thinking…" forever. Right: idle-timeout on `reader.read()` via Promise.race.
- `atelier/components/build/chat-pane.tsx:204-211` — anon `sessionId` only initialized in `useEffect` AFTER first render. First chat send (if user types instantly) sends `sessionId=''`. Right: lazy init in `useState` initializer (with SSR guard).
- `atelier/lib/anthropic/tools/generate_hero_image.ts:12-24` — `tryEvolveVibe()` builds `modulePath` via `['@', 'lib', 'vibe', 'evolve'].join('/')` to defeat static `import` resolution. **This is from packet 22 working around a missing packet 21.** Packet 21 is now merged. The dynamic-import trick is dead weight; should be a static import + the comment can go. Already noted in `_packets/COMMENTS.md` but the workaround should be deleted, not justified.
- `atelier/lib/vibe/extract-palette.ts:214-273` — JPEG branch uses byte-histogram (`bytes[i], bytes[i+1], bytes[i+2]` as RGB triples) which is **garbage**. JPEG is compressed; raw bytes aren't pixels. Most hero images uploaded are JPEG. Result: near-random palette. The whole reason packet 35 needs `node-vibrant`. Right: swap this entire decoder out for `node-vibrant`. STATE.md notes worker self-flagged this.
- `atelier/lib/email/send.ts:27-55` — Resend client built lazily once per process. `replyTo` is passed even when undefined; verify Resend SDK accepts that.

### → Packet 34 (Observability)

- **No Sentry**. `@sentry/nextjs` in deps, env vars declared, NOT wired in `instrumentation.ts` or `instrumentation-client.ts` or `next.config.mjs`. STATE.md acknowledges this.
- `atelier/instrumentation-client.ts:1-22` — only PostHog init. No Sentry browser SDK. No error reporting bridge from React error boundaries.
- `atelier/instrumentation.ts:1-3` — empty register; should `await import('./sentry.server.config')` and an edge counterpart.
- `atelier/lib/anthropic/observability.ts:33-87` — capture is called once per `message_end` event in the inner agent loop. Multi-tool turn fires multiple `$ai_generation` events. PostHog UI groups them under the same turn only via shared `peek_id+session_id` properties — but `tool_calls`, `tokens`, `latency` are per-iteration, not per-user-turn. Right: aggregate at the outer `chatTurn` boundary and emit a single `$ai_generation` per user turn (with `iterations` count as a property). STATE.md flags this as "30 — Single-fire `turn_end` SSE event" (same root cause).
- `atelier/components/build/share-sheet.tsx:117-208` — client share channels (`handleCopy`, `handleNativeShare`, anchor `<a>` for sms/whatsapp/twitter/facebook/email) do NOT fire `track('share_initiated')`. Only the server-side SMS/email send via `/api/share/send` fires. STATE.md confirms this carryover. The native-share, copy, twitter, facebook, whatsapp, iMessage clicks are invisible to analytics. Right: `trackFireAndForget` (via a client-side PostHog wrapper, since the facade is `server-only`).
- `atelier/lib/analytics/facade.ts` — `track()` is `'server-only'` (line 1). There is no client equivalent. Client code can call `posthog.capture(...)` direct from `instrumentation-client.ts` but loses the typed event taxonomy. Right: a `lib/analytics/client.ts` mirror of the discriminated union.
- `atelier/app/g/[slug]/opengraph-image.tsx` — `runtime = 'edge'` + dynamic fetch on every request. No `revalidate`, no cache headers, no edge-runtime cache lookup. Each viewer hit = one Supabase RPC + one Anthropic-OG render (free per Next docs but uses bandwidth). Right: Next 16 `revalidate = 3600` per slug + Supabase cache-control header passthrough.
- `atelier/app/api/posthog/[...path]/route.ts:13-39` — PostHog reverse proxy strips `Set-Cookie`/`Vary`/CSP headers from PostHog responses. PostHog session-recording uses cookies; this proxy will likely break recording. Untested with real PH traffic (STATE.md confirms).
- `atelier/app/g/[slug]/opengraph-image.tsx:88-95` — fallbackResponse() returns a Peek-less OG on `peek=null` OR on any Supabase error. No distinction in logs. Right: structured log on `catch`.
- `atelier/lib/analytics/facade.ts:189-194` — `distinctIdFor` returns `anon-${sessionId}` or `anon-${peekId}` as a fallback. A signed-in user whose userId is null in a fire-and-forget call gets bucketed as anon. Right: only fall back if both null+null+null; otherwise use peekId scope.
- `atelier/lib/anthropic/observability.ts:48-56` — `$ai_input: payload.inputMessages` and `$ai_output_choices: payload.outputContent` send full message arrays to PostHog. These may contain PII (user note text, scraped page snippets, recipient name). Right: redact-by-default; allow opt-in via env flag.
- `atelier/lib/analytics/facade.ts:217-241` — events table insert wraps PostHog capture. Both can leak PII. No redaction layer for `payload.url`, `destination` (email), `note_md` source.
- `atelier/components/providers.tsx` exists but **`app/layout.tsx` does not wrap children in `<Providers>`**. Result: `QueryClientProvider`, `ThemeProvider` (next-themes), `PostHogProvider` are all dead code. PostHog client-side capture happens directly via `instrumentation-client.ts` init only — no React context, no consent handling. CRITICAL.
- `atelier/lib/jobs/webhook-logger.ts:13-17` — webhook log row stores `payload` raw. Stripe webhooks contain payment-method last4, billing address, email. PII leak to a long-lived audit table. Right: redact PII before insert (no PAN data, but emails should be hash-only).
- No tracing across cron → scrape-worker → /api/scrape → Anthropic. Single-process trace_id not propagated.

### → Packet 35 (Quality / a11y / mobile / SEO)

- `atelier/lib/vibe/extract-palette.ts` — entire 415-line module being replaced by `node-vibrant` per packet 35. Delete on integration.
- `atelier/components/recipient/cinematic-reveal.tsx:22-67` — does NOT respect `prefers-reduced-motion`. Setting "reduce motion" in OS still plays the full 2+ second reveal animation. The `preview-sheet.tsx` does respect it (`useReducedMotion`). Inconsistent.
- `atelier/components/build/share-sheet.tsx` + `atelier/components/build/chat-pane.tsx` — both have inline brand-icon SVGs (XIcon, FacebookIcon) sans `<title>` for screen readers; rely on parent `<a aria-label>` (good in share-sheet) but no fallback if Lucide ever 404s.
- `atelier/components/recipient/cinematic-reveal.tsx:81-87` — `<motion.div ... role="button" tabIndex={0} aria-label="Tap to skip"` — entire-page click target. Skip is bound to Enter+Space, good. But the outer `<div role="button">` is the whole viewport-sized overlay — screen reader users will hit a no-text "button" of unknown purpose. Right: a real skip button.
- `atelier/components/build/share-sheet.tsx:269-313` — share-row tiles use `<a class="flex flex-col items-center..." aria-label="Share via X">` — no `:focus-visible` styling, default browser outline only. On dark surfaces the outline is invisible. Right: explicit focus ring tokens.
- `atelier/components/recipient/recipient-view.tsx:147-149` — `<main aria-label='A peek for ${name}'>` — `<main>` should have an implicit role; doesn't need aria-label. The recipient name (a person's name) ends up in the accessible name. Privacy + tone smell.
- `atelier/app/page.tsx` — landing page is 25 lines, single `<h1>`, two links. No `<meta description>`, no OG image, no `next-sitemap`, no robots tag. Right: SEO metadata.
- `atelier/app/layout.tsx:13-16` — `metadata.title = 'peek.gift'` is static; `description` is one sentence; no `metadataBase`, no canonical, no theme-color, no `manifest.json`, no Apple touch icon, no opengraph default image. Right: full metadata block in `app/layout.tsx`.
- `atelier/app/page.tsx:9-19` — "Start a Peek" → `/build`. `/build` is gated behind Clerk auth — anon visitors get redirected to sign-in instantly. Right: a public landing experience that explains what Peek is before paywall.
- `atelier/components/ui/input.tsx`, `textarea.tsx`, `button.tsx` — touch-target audit: Buttons `size="sm"` are 9×9 (h-9 = 36px). iOS HIG = 44pt, Material = 48dp. Right: size lift on mobile breakpoint.
- `atelier/components/build/share-sheet.tsx:269-313` — share tiles `p-3` text-xs; on a 320px-wide phone, 5-column grid (`sm:grid-cols-5`) is fine, but `grid-cols-2` for narrowest viewport means iMessage + WhatsApp + X + FB + Email shown 2-per-row. Tap targets cramped but technically meet 44px.
- `atelier/components/build/chat-pane.tsx:572-587` — empty-chat hint copy "Tell Peek who this is for and what they love…" — the textarea placeholder matches. No real onboarding state.
- `atelier/components/build/preview-sheet.tsx` — built entirely from scratch with framer-motion drag. No real-device testing per STATE.md notes. Touch friction on safari iOS for `touchAction: 'none'` body — interaction may swallow gestures.
- `atelier/app/build/[peekId]/page.tsx:138-145` — `ownedByAnon` branch references `anonymous_session_id` on metadata, but the only path that *creates* a peek (`app/build/page.tsx`) requires auth. **There is no anon-peek-creation path.** Either dead code or planned-but-unbuilt. STATE.md treats "anonymous metering" as an open architectural question.
- `atelier/components/build/chat-pane.tsx:419-431` — chat history loads ALL persisted messages on every page mount. No pagination. A long-running peek with 500 messages = ~500 jsonb rows hitting the client. Right: paginate `loadChatHistory` to last N, lazy-load older.
- `atelier/app/build/[peekId]/publish/share/page.tsx:96` — `redirect('/build/${peekId}/publish/checkout')` if status not published. But `/build/[peekId]/publish/checkout/page.tsx` doesn't exist. A user who lands on `/share` mid-publish gets a 404. Right: redirect to `/publish` instead.
- `atelier/app/g/[slug]/page.tsx:139-154` — `ensureRecipientSession` sets cookie with `sameSite: 'lax'`. Recipient may open the share link from native iMessage → opens in-app browser. Right: verify `sameSite: 'none'; secure: true` for cross-context openings, audit cookies.
- `atelier/components/recipient/cinematic-reveal.tsx:50-53` — `setPhase('done'); onDone()` on timer regardless of whether the user has interacted. If user tabs away, when they return the animation has already self-completed. Right: pause-on-visibility-change.
- `atelier/components/recipient/cinematic-reveal.tsx:95-103` — uses CSS `linear-gradient(..., url(${peek.heroImageUrl}))` injected into inline style. Hero URL is from DB and could contain `)` injection. Right: encode-then-injection-safe URL building.
- `atelier/components/build/chat-pane.tsx:299-307` — anon turn limit response: handles only `body.error === 'signup_required'`. Other 401 (e.g. unauth peek access) doesn't surface a useful message.
- `atelier/components/recipient/realtime.ts:142-145` — `useMemo(() => ({ setPending, refresh }), [setPending, refresh])` — `mutate` only memoizes the *object identity*, not the functions themselves. They're already memoized via useCallback so fine, but the layer is redundant.
- `atelier/components/build/chat-pane.tsx:64-76` — tool-name humanizer is regex-based and not localizable. Right: keyed map.

### → New follow-up packet needed

- **CRITICAL FUNCTIONAL: Inngest scrape worker calls HTTP route requiring Clerk auth.** `lib/jobs/scrape-worker.ts` fetches `${env.APP_URL}/api/scrape` but `/api/scrape` requires `getUserId()` → returns 401 from Inngest worker (no Clerk cookies). The scrape pipeline is BROKEN today. Either (a) make `/api/scrape` accept an Inngest signature OR (b) call `scrapePipeline()` directly from the worker. STATE.md notes acknowledges retries-to-fail but doesn't have a fix-packet.
- **CRITICAL FUNCTIONAL: `<Providers>` never wired into root layout.** `components/providers.tsx` exports a React Query / Theme / PostHog provider tree but `app/layout.tsx` does not use it. React Query (in deps) is unreachable. next-themes is unreachable. PostHog client provider is unreachable — autocapture works via direct init in `instrumentation-client.ts`, but `usePostHog()` hook calls anywhere would error. Right: one-line fix in `app/layout.tsx` to wrap `<Providers>`.
- **Migration scope cleanup.** `db/migrations/0002_schema_exposure.sql` grants anon/authenticated full DML on every peek_v2 table. Tighten to SELECT-only on read-needed tables; revoke INSERT/UPDATE/DELETE entirely from anon.
- **Slug collision.** `app/build/page.tsx:7-9` 12-hex-char slug + no retry on unique-violation. Lift to 16+ chars and retry-on-409. Cheap, one-file.
- **Anon-peek path doesn't exist.** `lib/chat/session.ts:80-100` branches on `metadata.anonymous_session_id` to allow anon access. `app/build/page.tsx` only creates peeks for authed users. Either build the anon flow (and re-think anon-turn cap CRITICAL above) or delete the branch and `assertPeekAccess` always requires authed curator.
- **README drift.** `atelier/README.md:2` claims "Next.js 15 + TS strict + Tailwind v3". Reality: Next 16, TS 6, Tailwind v4. Right: refresh.
- **Toast bug.** `components/ui/use-toast.ts:9` `TOAST_REMOVE_DELAY = 1000000` is the legendary shadcn copy-paste bug. Lower to `4_000`. One-line fix.
- **`tools/scrape_url.ts` doesn't `track('scrape_complete')` on dispatch success.** Only fires the `_failed` variant. So success rate of scrape-tool-call is observability-invisible.
- **`/api/upload` ALLOWED set includes `image/heic`/`image/heif` but Anthropic image source only supports jpg/png/gif/webp.** Curator uploads a HEIC, it gets stored, but downstream image-in-chat fails. Either reject HEIC at upload or transcode at upload (Sharp not on deps, would need a server-side decoder).
- **`db/migrations/meta/_journal.json`** is hand-maintained. Inevitable conflict on next `drizzle-kit generate`. Pre-emptively regenerate from snapshots.
- **`add_card.ts:108` position is single-shot read-and-increment** — race-on-concurrent-add yields duplicate positions. Right: `MAX(position) + 1` in a single SQL statement.
- **Stripe checkout `success_url` injects `{CHECKOUT_SESSION_ID}` placeholder.** That's Stripe-template syntax (good). But cancel_url `/build/${peekId}` lets a curator who cancels mid-payment land back at the editor without any "you cancelled" signal. Cosmetic but a Stripe-best-practice gap.
- **`app/api/share/send/route.tsx`** filename ends `.tsx` while every other route uses `.ts`. The file does contain JSX (the email-react render) so `.tsx` is correct, BUT inconsistency complicates path searches.
- **`lib/anthropic/tools/scrape_url.ts:14-15`** Output type imports affiliate network enum inline; should re-export from a single types module.

## Anti-findings (things checked, no shortcut found)

- **Stripe webhook signature validation** is using `stripe.webhooks.constructEvent(...)` correctly with the SDK's built-in tolerance/signature check. Good.
- **Svix verification** (`Webhook.verify`) is canonical. Good.
- **Skimlinks HMAC** uses `crypto.timingSafeEqual` correctly via `safeEqual()` helper. Constant-time. Good. (Signature format itself is unverified per STATE.md, but the verification code is sound.)
- **fal.ai content_type** propagated through rehost — no leakage of fal URLs into client.
- **Anthropic prompt-cache** (`cache_control: { type: 'ephemeral' }` on static system block) is wired correctly per SDK 0.98 GA.
- **Drizzle migration journal** snapshot ids unique (0001 collision was fixed in batch 3).
- **Stripe webhook idempotency** — checkout.session.completed branch IS idempotent due to status short-circuit. Only the OTHER (future) event types lack it.
- **OG image fallback** does serve when no peek found, doesn't throw. Good.
- **Tool-input Zod parse** runs in every tool handler before any DB write. Good defensive parse-don't-validate pattern.
- **`server-only`** is imported in every file that should be server-only (analytics facade, observability, scrape, image-gen, auth, supabase service/server). Good.
- **Stream cleanup** in `chat/route.ts` uses `safeClose` + `safeEnqueue` to handle the controller-closed case. Good.
- **CSRF** is implicitly handled because POST routes are JSON-only and authed via Clerk session — but no explicit origin check exists (would need packet 31's origin allowlist if introduced). Not flagging because session-cookie + same-origin is the de-facto guard.
- **`lib/security/origin.ts`**, **`lib/security/idempotency.ts`**, **`lib/security/recipient.ts`**, **`lib/rate-limit/redis.ts`**, **`lib/logger/index.ts`**, **`lib/retry/withRetry.ts`** — files seen during initial scan via parent-worktree leakage. **None are on `atelier-integration` HEAD.** Packet 31 is starting from zero on security helpers; packet 33 starts from zero on logger/retry; packet 31 starts from zero on rate-limit. Orchestrator should verify the worktree state in any worker's audit before merging.
