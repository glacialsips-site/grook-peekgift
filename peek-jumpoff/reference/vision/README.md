# vision/ — end-vision reference (DATED; not authority)

The three docs Frank handed over for the *end vision* (concept, requirements/spec grounded in the live peek_v2 schema, backend/vendor catalog). Per Frank these are **dated** — reference for direction, NOT instructions; current build decisions live in /DECISIONS.md and the contract. Key signals folded into the build: transparent floating chat over the live preview; vibe/aesthetic is the moat (the aesthetic quality gate is the open gap); rules engine as first-class data; runtime design tokens (no Tailwind); the model is the resolver.
